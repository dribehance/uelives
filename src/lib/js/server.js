(function() {
	// server side render js, freemark or other backend template
	localStorage.setItem("ls.server", JSON.stringify({
		wechat: "",
	}));
	// localStorage.setItem("ls.token", "\"${token}\"");
})();